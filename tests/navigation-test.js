import React from 'react';
import {
    NavigationReducible
} from '../src/index.js';
import {
    shallow,
    mount,
    render
} from 'enzyme';
import {
    expect
} from 'chai';
import sinon from 'sinon';

const {
    describe,
    it
} = global;

describe('Navigation Reducible', () => {
    it('shall not show dropdown button on full size', () => {
    });
    it('shall show dropdown when not all items visible anymore', () => {
    });
    it('shall show dropdown on button click', () => {
    });
    it('shall hide dropdown on body click', () => {
    });
});
